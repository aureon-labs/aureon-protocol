# AUREON PROTOCOL

Prototype system for routing data through multiple modules.

## Structure
- aureon-core : routing engine logic
- aureon-network : switching logic
- api : simple example combining both modules

## How to run (example)
python3 api/route_example.py
